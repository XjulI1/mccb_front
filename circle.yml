## Customize the test machine
machine:
  timezone:
    Europe/Paris

  # Version of node to use
  node:
    version:
      6.10.0

deployment:
  production:
    branch: master
    commands:
      - ./deploy.sh

