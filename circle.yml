## Customize the test machine
machine:
  timezone:
    Europe/Paris

  # Version of node to use
  node:
    version:
      8.2.1

deployment:
  production:
    branch: master
    commands:
      - ./deploy.sh

